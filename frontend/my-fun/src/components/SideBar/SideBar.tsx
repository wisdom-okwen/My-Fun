import React from "react";
import { styles } from "./SideBar.styles";

const SideBar: React.FC = () => {
    return (
        <div style={styles}>
            This is the SideBar
        </div>
    );
};

export { SideBar };
